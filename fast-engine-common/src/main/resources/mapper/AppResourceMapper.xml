<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.coolcollege.fast.storage.mapper.AppResourceMapper">

    <select id="getAppResourceType" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT
        t.resource_type
        FROM
        fast_engine_app_resource t
        WHERE
        t.app_id = #{appId,jdbcType = VARCHAR}
        <if test="domainId!=null and domainId!=''">
            and
            t.domain_id = #{domainId,jdbcType = VARCHAR}
        </if>
    </select>

    <select id="getAppAllDomain" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT
        t.domain_id
        FROM
        fast_engine_app_resource t
        WHERE
        t.app_id = #{appId,jdbcType = VARCHAR}
    </select>
    <select id="getIncludeClassifyResourceType" resultType="java.lang.String">
        SELECT
        t.resource_type
        FROM
        fast_engine_app_resource t
        WHERE
        t.app_id = #{appId,jdbcType = VARCHAR}
        AND
        t.include_classify = #{flag, jdbcType = BOOLEAN}
    </select>

</mapper>